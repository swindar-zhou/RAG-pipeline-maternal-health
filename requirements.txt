# Web scraping
requests==2.31.0
beautifulsoup4==4.12.3
lxml==5.1.0
aiohttp>=3.9.0          # async HTTP — used in scraper_discovery.py and scraper_extract.py
duckduckgo-search>=5.0  # DDG search API — used in scraper_discovery.py

# LLM API clients
openai==1.12.0
anthropic==0.18.1

# Utilities
python-dotenv==1.0.1
httpx==0.27.2
pydantic>=2.9.0,<3.0.0

# LangChain / LangGraph (Phase 2 enhanced scraping + conversational ZIP→programs graph)
# Install separately when starting Phase C work: pip install -r requirements-langchain.txt
# Listed here for reference — not required for Phase 1/2/3 pipeline.
langchain>=0.2.0
langchain-community>=0.2.0
langchain-openai>=0.1.0
langgraph>=0.1.0
chromadb>=0.5.0           # local vector store (per-county collections)
pgeocode>=0.4.0           # offline ZIP→county mapping (USPS dataset, no API key)
pypdf>=4.0.0              # PDF text extraction
pdfplumber>=0.10.0        # PDF table/layout extraction (fallback for pypdf)
playwright>=1.44.0        # headless browser for the 10 counties that block aiohttp (403)

# Testing
pytest==7.4.3
pytest-mock==3.12.0
pytest-cov==4.1.0
