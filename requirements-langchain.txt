# Phase C dependencies — LangChain/LangGraph enhanced pipeline
# Install with: pip install -r requirements-langchain.txt
# Requires the base requirements.txt to already be installed.
#
# After pip install, run once to download the Chromium binary:
#   playwright install chromium
#
# curl-cffi (bot-bypass Tier 1) is already in requirements.txt.
# Playwright (Tier 2) is listed below for the 12 bot-blocked counties.

-r requirements.txt

langchain>=0.2.0
langchain-community>=0.2.0
langchain-openai>=0.1.0
langgraph>=0.1.0
chromadb>=0.5.0
pgeocode>=0.4.0
pypdf>=4.0.0
pdfplumber>=0.10.0
playwright>=1.44.0
