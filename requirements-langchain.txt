# Phase C dependencies — LangChain/LangGraph enhanced pipeline
# Install with: pip install -r requirements-langchain.txt
# Requires the base requirements.txt to already be installed.
#
# Playwright also needs a one-time browser install after pip install:
#   playwright install chromium

-r requirements.txt

langchain>=0.2.0
langchain-community>=0.2.0
langchain-openai>=0.1.0
langgraph>=0.1.0
chromadb>=0.5.0
pgeocode>=0.4.0
pypdf>=4.0.0
pdfplumber>=0.10.0
playwright>=1.44.0
